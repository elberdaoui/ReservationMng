@*@model IEnumerable<ReservationMng.Models.Reservation>*@

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
    var lst = ViewBag.list;
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
    <tr>
        @*<th>
           UserName
        </th>*@
        @*<th>
            Class
        </th>
        <th>
            Name
        </th>*@
        <th>
           Cause
        </th>
        <th>
           Date
        </th>
        <th>
           Status
        </th>
        <th></th>
    </tr>
    </thead>
    <tbody>
@foreach (var item in lst) {
        <tr>
            @*<td>
               @item.Students.UserName
            </td>*@
            @*<td>
               @item.Students.Class
            </td>
            <td>
              @item.name
                
            </td>*@
            <td>
             @item.Cause
            </td>
            <td>
             @item.Date
            </td>
            <td>
             @item.Status
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
